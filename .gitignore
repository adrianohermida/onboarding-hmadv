(onboarding-hmadv is a single modified file that appears to be a source/config file based on the name, and there are no build artifacts, dependencies, or temp files in the changes - so no ignores are needed)

(empty)
node_modules
