# Security Contract: Permissions

All modules must:

- map route/action to scoped permissions
- enforce tenant + ownership checks before sensitive operations
- log denied and escalated access attempts
