'use client';

// Global modal portal root — wire up Radix Dialog or custom modals here.
// Currently a placeholder; import and mount in layout when first modal is needed.

export default function ModalRoot() {
  return null;
}
