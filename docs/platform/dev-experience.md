# Developer Experience Foundation

Standard commands:

- npm run bootstrap:dev
- npm run validate:platform
- npm run diagnostics:platform
- npm test

Guidelines:

- keep environment configuration isolated per environment
- run validation scripts before opening PR
- use CI pipeline output as source of truth for merge readiness
