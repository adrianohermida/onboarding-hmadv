import { useMemo } from "react";

export function usePublicacoesScreenBodyProps(params) {
  return useMemo(() => ({
    actionState: params.actionState,
    activeJobId: params.activeJobId,
    adviseBackfillProgress: params.adviseBackfillProgress,
    adviseCursor: params.adviseCursor,
    adviseLastCycleTotal: params.adviseLastCycleTotal,
    adviseLastRunAt: params.adviseLastRunAt,
    adviseMode: params.adviseMode,
    advisePersistedDelta: params.advisePersistedDelta,
    adviseSync: params.adviseSync,
    adviseTokenOk: params.adviseTokenOk,
    backendHealth: params.backendHealth,
    blockingJob: params.blockingJob,
    canManuallyDrainActiveJob: params.canManuallyDrainActiveJob,
    candidateQueueErrorCount: params.candidateQueueErrorCount,
    candidateQueueMismatchCount: params.candidateQueueMismatchCount,
    copilotContext: params.copilotContext,
    data: params.data,
    drainInFlight: params.drainInFlight,
    executionHistory: params.executionHistory,
    formatFallbackReason: params.formatFallbackReason,
    formatSnapshotLabel: params.formatSnapshotLabel,
    getOperationalPlanStepState: params.getOperationalPlanStepState,
    handleAction: params.handleAction,
    hasBlockingJob: params.hasBlockingJob,
    hasMultipleBlockingJobs: params.hasMultipleBlockingJobs,
    healthSuggestedActions: params.healthSuggestedActions,
    isDockedPublicacoesView: params.isDockedPublicacoesView,
    isLightTheme: params.isLightTheme,
    isResultView: params.isResultView,
    jobs: params.jobs,
    latestHistory: params.latestHistory,
    latestJob: params.latestJob,
    latestRemoteRun: params.latestRemoteRun,
    limit: params.limit,
    loadOverview: params.loadOverview,
    loadPartesCandidates: params.loadPartesCandidates,
    loadProcessCandidates: params.loadProcessCandidates,
    noPublicationActivityTypeConfigured: params.noPublicationActivityTypeConfigured,
    operationalPlan: params.operationalPlan,
    operationalStatus: params.operationalStatus,
    partesBacklogCount: params.partesBacklogCount,
    partesPage: params.partesPage,
    pendingJobCount: params.pendingJobCount,
    processCandidates: params.processCandidates,
    processNumbers: params.processNumbers,
    processPage: params.processPage,
    publicationActivityTypeHint: params.publicationActivityTypeHint,
    publicationActivityTypes: params.publicationActivityTypes,
    queueRefreshLog: params.queueRefreshLog,
    queuesViewModel: params.queuesViewModel,
    remoteHealth: params.remoteHealth,
    remoteHistory: params.remoteHistory,
    runOperationalPlanStep: params.runOperationalPlanStep,
    runPendingJobsNow: params.runPendingJobsNow,
    selectedPartesKeys: params.selectedPartesKeys,
    selectedProcessKeys: params.selectedProcessKeys,
    selectedProcessNumbers: params.selectedProcessNumbers,
    selectedUnifiedNumbers: params.selectedUnifiedNumbers,
    setLimit: params.setLimit,
    setProcessNumbers: params.setProcessNumbers,
    snapshotMesaIntegrada: params.snapshotMesaIntegrada,
    snapshotPartes: params.snapshotPartes,
    snapshotProcessos: params.snapshotProcessos,
    syncWorkerLastPublicacoes: params.syncWorkerLastPublicacoes,
    updateView: params.updateView,
    view: params.view,
  }), [params]);
}
