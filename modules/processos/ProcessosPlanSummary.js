import { StatusBadge } from "./ui-primitives";

export default function ProcessosPlanSummary({ getOperationalPlanStepState, isLightTheme, operationalPlan, runOperationalPlanStep }) {
  if (!operationalPlan.length) return null;
  return <div className={`mt-4 rounded-[22px] border p-4 text-sm ${isLightTheme ? "border-[#d7d4cb] bg-[rgba(255,255,255,0.82)] text-[#1f2937]" : "border-[#2D2E2E] bg-[rgba(4,6,6,0.35)]"}`}><p className={`text-[11px] font-semibold uppercase tracking-[0.18em] ${isLightTheme ? "text-[#6b7280]" : "opacity-60"}`}>Plano operacional enxuto</p><div className="mt-3 grid gap-3 md:grid-cols-3">{operationalPlan.slice(0, 3).map((step, index) => { const state = getOperationalPlanStepState(step, index); return <button key={`${step.title}-${index}`} type="button" onClick={() => runOperationalPlanStep(step)} className={`rounded-[18px] border p-3 text-left hover:border-[#C5A059] ${isLightTheme ? "border-[#d7d4cb] bg-white" : "border-[#2D2E2E] bg-[rgba(5,7,6,0.72)]"}`}><div className="flex flex-wrap items-center justify-between gap-2"><p className={`text-[10px] uppercase tracking-[0.16em] ${isLightTheme ? "text-[#9a6d14]" : "text-[#C5A059]"}`}>Passo {index + 1}</p><StatusBadge tone={state.tone}>{state.label}</StatusBadge></div><p className="mt-2 font-semibold">{step.title}</p><p className={`mt-2 text-xs ${isLightTheme ? "text-[#4b5563]" : "opacity-70"}`}>{step.detail}</p></button>; })}</div></div>;
}
